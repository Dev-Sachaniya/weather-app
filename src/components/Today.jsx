import React from "react";
import axios from "axios";
import { Typography } from "@mui/material";

const Today = () => {
  return (
    <div>
      <Typography
        sx={{ fontFamily: "Poppins", color: "black", textAlign: "center" }}
      >
        Today Will be done tommorrow
      </Typography>
    </div>
  );
};

export default Today;
